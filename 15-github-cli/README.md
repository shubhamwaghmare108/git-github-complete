# 15 — GitHub CLI

Install GitHub CLI (`gh`) separately from Git.

Useful commands:
```bash
gh auth login
gh repo clone OWNER/REPO
gh repo view --web
gh issue create
gh issue list
gh pr create
gh pr list
gh pr checkout <number>
gh run list
gh run watch
```

Use `gh help` and `gh <command> --help` to discover options.