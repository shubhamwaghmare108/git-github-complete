# 07 — Remote Git

```bash
git remote -v
git remote add origin <url>
git fetch origin
git pull --rebase origin main
git push -u origin main
git branch -r
git push origin --delete feature/login
```

Understand the difference between local branches, remote-tracking branches such as `origin/main`, and the actual remote repository.