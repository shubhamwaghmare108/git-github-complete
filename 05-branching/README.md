# 05 — Branching

```bash
git branch
git switch -c feature/login
git switch main
git branch -m old-name new-name
git branch -d feature/login
```

A branch is a movable reference to a commit. Use short-lived feature branches for isolated work.

## Lab
Create two branches, make different commits, merge one branch, and intentionally create a conflict on another.