# GitHub Cheat Sheet

Core objects: repository, branch, fork, issue, pull request, review, project, release, action, environment.

Typical flow:
`Issue → branch → commit → push → PR → review → CI → merge → release`

CLI:
```bash
gh auth status
gh repo view
gh issue list
gh pr list
gh pr create
gh run list
```